// Types test: primitives, arrays
class Types {
    static function main() {
        var x:Int = 42;
        var y:Float = 3.14;
        var s:String = "hello";
        var b:Bool = true;
        var arr:Array<Int> = [1, 2, 3];
        trace(x + y);
        trace(s);
        trace(arr.length);
    }
}
